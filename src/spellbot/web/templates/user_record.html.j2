{% extends "record_base.html.j2" %}
{% block table %}
<table>
    <thead>
        <tr class="table100-head">
            <th>Game</th>
            <th>Time</th>
            <th>Guild</th>
            <th>Channel</th>
            <th>Format</th>
            <th>Seats</th>
            <th>Bracket</th>
            <th>Locale</th>
            <th>Link</th>
            <th>Players</th>
            <th>Jump</th>
        </tr>
    </thead>
    <tbody id="rows">
        {% for record in records %}
            <tr>
                <td><a href="/game/{{record.id}}">SB#{{record.id}}</a></td>
                {% if tz_offset and tz_name %}
                    <td>{{record.updated_at | humanize(tz_offset, tz_name)}}</td>
                {% else %}
                    <td class="timestamp">{{record.updated_at}}</td>
                {% endif %}
                <td><a href="https://discord.com/channels/{{record.guild}}">{{record.guild_name}}</a></td>
                <td><a href="https://discord.com/channels/{{record.guild}}/{{record.channel}}">#{{record.channel_name}}</a></td>
                <td>{{record.format}}</td>
                <td>{{record.seats}}</td>
                <td>{{record.bracket}}</td>
                <td>{{record.locale}}</td>
                {% if record.link %}
                    <td><a href="{{record.link}}">Open in {{record.service}}</a><span class="data">{{record.link}}</span></td>
                {% else %}
                    <td>&nbsp;</td>
                {% endif %}
                <td class="scores">
                    <ul>
                        {% for name, data in record.scores.items() %}
                            <li>
                                <div class="tooltip">
                                    {{name | default('Unknown User', true)}}
                                    <span class="tooltiptext">
                                        <span class="tooltipcta">Click&nbsp;ID&nbsp;to&nbsp;copy:</span><br />
                                        <a onclick="copy(this)" class="button user_id" style="pointer-events: auto; cursor: pointer;">{{data[0]}}</a>
                                    </span>
                                </div>
                            </li>
                        {% endfor %}
                    </ul>
                    <span class="data">
                        {% for name, data in record.scores.items() %}{{name}}:{{data[0]}}={{data[1]}};{% endfor %}
                    </span>
                </td>
                <td><a href="https://discord.com/channels/{{record.guild}}/{{record.channel}}/{{record.message}}" class="button">Jump to game</a></td>
            </tr>
        {% endfor %}
    </tbody>
</table>
{% endblock %}
